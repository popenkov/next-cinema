export const accentColor = '#E30B13';
export const bgColor = '#191B1F';

// если есть window, значит это клиент
export const IS_SERVER = typeof window === 'undefined';
export const IS_CLIENT = typeof window !== 'undefined';
